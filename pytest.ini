[pytest]
markers =
    integration: end-to-end integration tests

# A hung test should fail, not stall the run. The socket and PTY tests here can
# block indefinitely when a fix regresses (a blocking send against a peer that
# never reads, or a PTY whose consumer never opened it), and the CI job has no
# timeout of its own.
timeout = 60
