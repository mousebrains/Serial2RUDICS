#
# Automatically generated on @DATE@
# ARGS: @GENERATED@
#
# Map a serial port to a RUDICS port on a dockserver
#
# sudo cp USBToRUDICS@.service /etc/systemd/system/
#
# sudo systemctl daemon-reload
# sudo systemctl enable USBToRUDICS@ttyUSB0.service
#
# Feb-2020, Pat Welch, pat@mousebrains.com

[Unit]
Description=ttyUSB to RUDICS service on %I
BindsTo=dev-%i.device
After=dev-%i.device

[Service]
# Type=simple # Default
User=@USERNAME@
Group=@GROUPNAME@
WorkingDirectory=@DIRECTORY@
SyslogIdentifier=USBToRUDICS-%i
#
ExecStartPre=/bin/mkdir -p @DIRECTORY@
ExecStart=@EXECUTABLE@ \
	--host=@HOSTNAME@ \
	--port=@PORT@ \
	--serial=/dev/%i \
	--baudrate=@BAUDRATE@ \
	--idleTimeout=@TIMEOUT@ \
	--logfile=%i.log

TimeoutStopSec=30
RestartSec=@RESTARTSECONDS@
Restart=always

# Cap RSS per instance so a runaway buffer can't OOM a Pi 3B running
# multiple ports. Systemd will kill+restart on breach.
MemoryMax=@MEMORYMAX@

NoNewPrivileges=true
ProtectSystem=strict
ReadWritePaths=@DIRECTORY@
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictSUIDSGID=true
PrivateTmp=true

[Install]
WantedBy=multi-user.target
